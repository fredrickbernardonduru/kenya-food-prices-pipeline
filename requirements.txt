# psycopg2-binary is the only hard requirement for load.py
psycopg2-binary==2.9.9
 
# pandas — use whatever version is already in the base image
pandas
 
# HTTP requests (for extract step)
requests==2.31.0
 
# Optional utilities
python-dotenv==1.0.1